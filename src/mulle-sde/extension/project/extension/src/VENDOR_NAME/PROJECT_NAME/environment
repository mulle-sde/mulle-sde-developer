#
# Specify environment variables to be places into .mulle-env
# The format is strictly a single line with
# key in uppercase with underscores and a double quoted value
# The comments above the environment variable assignment, will be
# transferred to the .mulle-env file
#

#
# Assume we are not cmake backed, we should tell mulle-sde that our callback
# should run during UPDATE. Callbacks are comma-separated.
#
MULLE_SDE_UPDATE_CALLBACKS="foofiles"
